name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
env:
  NAME_RAO: ${{ vars.NAME_LOCAL }}
  DB_PORT: ${{ vars.DB_LOCAL }}
  USER_OS: ${{ vars.USER_LOCAL }}
  HOST_OS: ${{ vars.HOST_LOCAL }}
  PASSWORD_OS: ${{ vars.PASSWORD_LOCAL }}

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      run: |
        python manage.py test
