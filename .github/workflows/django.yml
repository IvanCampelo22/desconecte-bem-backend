name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
env:
  NAME_LOCAL: desconecte-bem
  PORT_LOCAL: desconecte-bem
  USER_LOCAL: desconecte-bem
  HOST_LOCAL: desconecte-bem
  PASSWORD_LOCAL: desconecte-bem
  EMAIL_PORT: desconecte-bem
  EMAIL_HOST: desconecte-bem
  EMAIL_HOST_USER: desconecte-bem
  EMAIL_HOST_PASSWORD: desconecte-bem

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.12]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      run: |
        python manage.py test
      env: 
        DATABASE: |
          default:
            ENGINE: django.db.backends.postgresql
            NAME: ${{ env.NAME_LOCAL }}
            USER: ${{ env.USER_LOCAL }}
            PASSWORD: ${{ env.PASSWORD_LOCAL }}
            HOST: ${{ env.HOST_LOCAL }}
            PORT: ${{ env.PORT_LOCAL }}
